const expect = require('chai').expect;

describe('', () => {
  describe('Empty Test:', () => {
    it('1 should be a number', (done) => {
      expect(typeof 1).to.equal('number');
      done();
    }); 
  });
});

